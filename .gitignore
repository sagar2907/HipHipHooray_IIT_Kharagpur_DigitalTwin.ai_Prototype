# Generated datasets — regenerable from the seeded builders in scripts/.
# ~765 MB of derived data; the generators are what needs versioning.
# Root-anchored on purpose: a bare `dataset/` also matches docs/dataset/,
# which silently hid the dataset documentation from the repo once already.
/dataset/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Environments
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp

# server scratch
nohup.out
/results/live/
/results/twin.db
/results/twin.db-wal
/results/twin.db-shm

# raw .webm from scripts/record_demo.py — the converted .mp4 is what's committed
/10_Demo_Video/_raw/
