# DigitalTwin.ai — prototype runtime.
# Everything needed to run `python web/server.py` from a clean clone.
# Version floors are conservative; the tested version is noted beside each.

fastapi>=0.115          # tested on 0.136.3
uvicorn>=0.30           # tested on 0.49.0
pandas>=2.2             # tested on 3.0.3
numpy>=1.26             # tested on 2.4.6

# Not required to run the demo — see requirements-dev.txt for the test suite,
# the deck builder and the Round 1 plotting script.
